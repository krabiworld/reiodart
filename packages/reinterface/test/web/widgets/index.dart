import 'package:reio/widget.dart';
import 'package:reio/widget_html.dart';

class Index extends Widget {
  Index(super.construct, super.styles);
}

Widget index = Index((w) => Div('Index content!'), () => null);
